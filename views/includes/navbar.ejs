<div class=" sticky-top container-fluid">
  <nav class="navbar navbar-expand-lg bg-body-light border-bottom container">
    <div class="container-fluid">
      <a class="navbar-brand" href="/"
        ><i class="fa-brands fa-airbnb me-1" style="color: #df2086"></i
        >airbnb</a
      >
      <button
        class="navbar-toggler"
        type="button"
        data-bs-toggle="collapse"
        data-bs-target="#navbarSupportedContent"
        aria-controls="navbarSupportedContent"
        aria-expanded="false"
        aria-label="Toggle navigation"
      >
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item">
            <a class="nav-link" href="/">Home</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/api/list">All Listings</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/api/list/new">Add New Listing</a>
          </li>
        </ul>

        <!-- Example split danger button -->

        <form class="d-flex" role="search">
          <input
            class="form-control me-2"
            type="search"
            placeholder="Search"
            aria-label="Search"
          />
          <button class="btn btn-outline-success" type="submit">Search</button>
        </form>

        <div class="btn-group ms-auto me-5">
          <!-- <button type="button" class="btn btn-light"></button> -->
          <button
            type="button"
            class="btn btn-primary dropdown-toggle dropdown-toggle-split"
            data-bs-toggle="dropdown"
            aria-expanded="false"
          >
            <span class="visually-hidden">Toggle Dropdown</span>
            <i class="fa-solid fa-user fa-lg" style="color: #ffd43b"></i>
          </button>

          <ul class="dropdown-menu">
            <% if(currentUser === "no-user" && isAuthenticate === "no") { %>
            <li><a class="dropdown-item" href="/signup">SignUp</a></li>
            <li><a class="dropdown-item" href="/login">Login</a></li>
            <% } %> <% if(currentUser !== "no-user" && isAuthenticate === "yes") { %>
            <li><a class="dropdown-item" href="/logout">LogOut</a></li>
            <% } %>
          </ul>
        </div>
      </div>
    </div>
  </nav>
</div>
